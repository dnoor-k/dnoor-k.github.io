requirements.txt
1. install packages using !pip install (colab has most pre-installed)
python=3.1
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
jupyterlab>=4.0
ipywidgets>=8.0
black>=23.0
pytest>=7.0
matplotlib>=3.7
folium>=0.15
plotly>=5.18


data sourcing


URL: 
https://catalog.data.gov/dataset/pwd-stormwater-billing-parcels


Format: CSV for tabular, GeoJSON for Geometry Analysis


Upload Process for Google Colab: 
1. download GeoJSON/CSV
2. import into drive
3. drive.mount('/content/drive')
when uploading…
- `nrows=1000` #Load subset for testing
- `gdf.head(50)` # Work with sample for speed
